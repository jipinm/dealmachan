# DealMachan Merchant App — Test Environment
# Used by Vitest during test runs (not exposed to the browser)

# Point the API client at the MSW-intercepted local URL
VITE_API_URL=/api

# API origin — required by vite.config.ts (proxy target); MSW intercepts all requests in tests
VITE_API_ORIGIN=http://localhost
